<%= form_with(model: favorites_recipe, local: true) do |form| %>
  <% if favorites_recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorites_recipe.errors.count, "error") %> prohibited this favorites_recipe from being saved:</h2>

      <ul>
      <% favorites_recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :recipe %>
    <%= form.text_area :recipe %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
